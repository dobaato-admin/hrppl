diff --git a/dist/stacks/tanstack/vite.js b/dist/stacks/tanstack/vite.js
index 04c8e8dcefe282cf5ae3cc8330c727589639eee6..1b91a6cb432fdb863d4bec5bb6ed3b051d1fc774 100644
--- a/dist/stacks/tanstack/vite.js
+++ b/dist/stacks/tanstack/vite.js
@@ -11,7 +11,9 @@ function normalizePath(p) {
 	return p.split(sep).join("/");
 }
 function assertContains(parent, child, label) {
-	if (child !== parent && !child.startsWith(parent + sep)) throw new Error(`@lovable.dev/mcp-js: ${label} must resolve under ${parent}, got ${child}`);
+	parent = normalizePath(parent);
+	child = normalizePath(child);
+	if (child !== parent && !child.startsWith(parent + "/")) throw new Error(`@lovable.dev/mcp-js: ${label} must resolve under ${parent}, got ${child}`);
 }
 function wellKnownRouteFile(routesDir, urlPath) {
 	const segments = urlPath.replace(/^\//, "").split("/");
